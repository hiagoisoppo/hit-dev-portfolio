'use client'
import { motion } from 'framer-motion'

export default function Contact() {
  return (
    <motion.section className="flex h-screen w-screen justify-center bg-green-300">
      <h1 className="text-6xl">CONTACT</h1>
    </motion.section>
  )
}
