'use client'
import { motion } from 'framer-motion'

export default function Projects() {
  return (
    <motion.section className="flex h-screen w-screen justify-center bg-green-500">
      <h1 className="text-6xl">PROJECTS</h1>
    </motion.section>
  )
}
