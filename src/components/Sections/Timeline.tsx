'use client'
import { motion } from 'framer-motion'

export default function TimeLine() {
  return (
    <motion.section className="flex h-screen w-screen justify-center bg-green-700">
      <h1 className="text-6xl">TIME LINE</h1>
    </motion.section>
  )
}
